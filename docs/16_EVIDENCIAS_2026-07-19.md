# 16 — Evidencias y capturas confirmadas (2026-07-19)

Este archivo registra qué partes fueron visibles en capturas o vídeos durante la sesión.

## BP_DungeonGenerator_V2 — panel de funciones

Captura confirmó 20 funciones propias:

```text
GenerateDungeon
ResetDungeon
InitRandomStream
CreateStartCell
IsCellOccupied
GetNeighborCoord
TryAddRandomCell
BuildDungeonLayout
SpawnRoomsFromCells
GetOppositeDirection
SetConnectionOnCell
DebugPrintLayout
DebugDrawConnections
DebugDrawDoorPoints
FindCellIndexByCoord
DebugDrawDoorToDoorConnections
SpawnCorridorsFromConnections
ChooseKeyAndBossCells
SpawnBossRoomDoors
SpawnDoorAtRoomDirection
```

## BP_DungeonGenerator_V2 — panel de variables

Captura confirmó categorías y variables:

```text
Pasillos:
CorridorDebugClass
SpawnedCorridors

Habitaciones:
CellSize
DungeonSeed
MaxGenerationAttempts
StartDebugRoomClass
SpawnedRooms
DungeonCells
RandomStream
MaxRooms
bUseRandomSeed
SelectedRoomClass

Debug:
DebugRoomClass
bGenerateonBeginPlay
bDebugDungeon

Boss Room:
BossCellIndex
BossGridX
BossGridY
BossDebugRoomClass

Key Room:
KeyCellIndex
KeyGridX
KeyGridY
KeyDebugRoomClass

Puertas:
BossDoorClass
SpawnedDungeonDoors
DungeonCellLinks
```

## BP_RoomMaster_Dungeon — panel

Se confirmó que la captura pertenece a `BP_RoomMaster_Dungeon`.

El original `BP_RoomMaster` no se ha modificado.

Funciones visibles:

```text
GenerateRoom
SetupFloor
SetupWall South
SetupWall East
SetupWall North
SetupWall West
SetupCeiling
UpdateConnectionMarkers
DecideRoomExits
RandomizeRoomSize
CenterRoomContentOnActorOrigin
UpdateRoomBounds
```

Interfaz visible:

```text
Get Room Bounds Data
Get Door World Location
Init Room from Cell
```

Componentes visibles:

```text
RoomBounds
Arrow_Entrance_South
Arrow_Exit_West
Arrow_Exit_East
Arrow_Exit_North
ChildActor_Wall_North
ChildActor_Ceiling
ChildActor_Floor
ChildActor_Wall_West
ChildActor_Wall_East
ChildActor_Wall_South
RoomContentRoot
Scene
```

## GenerateDungeon

Capturas confirmaron el cable de ejecución:

```text
GenerateDungeon
→ Switch Has Authority
→ ResetDungeon
→ InitRandomStream
→ CreateStartCell
→ BuildDungeonLayout
→ ChooseKeyAndBossCells
→ SpawnRoomsFromCells
→ SpawnCorridorsFromConnections
→ SpawnBossRoomDoors
→ DebugDrawDoorPoints
→ DebugDrawDoorToDoorConnections
```

## DebugPrintLayout

Capturas confirmaron:

```text
Branch bDebugDungeon
→ ForEach DungeonCells
→ Break ST_DungeonCell
→ Enum to String
→ Format Text
→ Print String
```

## TryAddRandomCell

La secuencia completa fue mostrada en varias capturas:

```text
Length <= 0
→ elegir Random Cell Index
→ Get DungeonCells
→ Set Selected Cell
→ elegir Random Direction Index
→ Select N/E/S/W
→ Set Selected Direction
→ Break Selected Cell
→ GetNeighborCoord
→ Set Neighbor X/Y
→ IsCellOccupied
→ Branch
→ New Room Seed
→ SetConnectionOnCell padre
→ Updated Selected Cell
→ Set Array Elem
→ Make New Base Cell
→ GetOppositeDirection
→ SetConnectionOnCell hija
→ New Connected Cell
→ Add DungeonCells
→ Add DungeonCellLinks
→ Return Added=true
```

La captura final confirmó:

```text
Random Cell Index → Parent Cell Index
Selected Direction → Direction from Parent
Has Parent = true
```

## Pruebas Cells/Links

Capturas confirmaron:

```text
Cells: 10 | Links: 10
Cells: 20 | Links: 20
Cells: 50 | Links: 50
```

El usuario confirmó además pruebas satisfactorias con:

```text
15 habitaciones
150 habitaciones
```

## Vídeos de links

Dos vídeos mostraron prints de:

```text
ChildIndex
ParentCellIndex
DirectionFromParent
bHasParent
```

Resultado observado:

```text
Child 0 → Parent -1, HasParent=false
Hijas → Parent anterior, HasParent=true
```

## Start una sola salida

Captura del primer montaje detectó dos errores:

```text
Return Added estaba true.
OR solo comprobaba South y West.
```

Se corrigió a:

```text
Return Added=false
OR con North, East, South y West
```

Captura posterior de la mazmorra mostró Start con un único pasillo.

El usuario confirmó pruebas satisfactorias con:

```text
10
15
20
50
150 habitaciones
```

## Captura visual del layout

Se observaron:

```text
Start morada/prebuilt.
Boss roja terminal.
Key morada/rosa especial.
Salas Normal procedurales.
Pasillos verdes.
DoorPoints debug de colores.
```

La colocación sigue siendo `Grid * CellSize`, por lo que esta evidencia no valida todavía tamaños variables sin solapamiento.

## Regla para usar estas evidencias

Estas evidencias confirman el estado del 2026-07-19.

Si una captura futura contradice este archivo:

```text
captura futura
→ actualizar docs/00_ESTADO_ACTUAL.md
→ actualizar documentación afectada
→ añadir entrada a CHANGELOG.md
```
